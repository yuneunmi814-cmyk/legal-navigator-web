name = "legalnavi"
pages_build_output_dir = "./dist"
compatibility_date = "2026-09-10"

# Pages-to-Worker calls use a service binding, not a public workers.dev fetch.
[[services]]
binding = "GUIDANCE"
service = "legalnavi-chat"
